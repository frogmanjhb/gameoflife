[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install",
  "cd server && rm -rf node_modules && npm install"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

