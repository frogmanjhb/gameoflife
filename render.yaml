services:
  - type: web
    name: gameoflife-backend
    env: node
    plan: free
    buildCommand: cd server && npm install && npm run build
    startCommand: cd server && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: gameoflife-db
          property: connectionString
      - key: CLIENT_URL
        value: https://gameoflife-5jf4.onrender.com

  - type: static
    name: gameoflife-frontend
    buildCommand: cd client && npm install && npm run build
    staticPublishPath: ./client/dist
    envVars:
      - key: NODE_ENV
        value: production

databases:
  - name: gameoflife-db
    plan: free
